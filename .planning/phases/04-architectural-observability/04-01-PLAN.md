---
phase: 04-architectural-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/routes/blueprint.py, main.py, frontend-v2/src/components/Navigation/PillNavbar.tsx, frontend-v2/src/App.tsx]
autonomous: true
requirements: [UI-06, UI-08]

must_haves:
  truths:
    - "Backend exposes /api/blueprint with Mermaid graph and dictionary"
    - "UI Navbar has a functional Blueprint tab"
    - "Switching to Blueprint tab hides operations and shows placeholder architecture view"
  artifacts:
    - path: "api/routes/blueprint.py"
      provides: "Blueprint metadata API"
    - path: "frontend-v2/src/App.tsx"
      provides: "View state management (Operations vs Architecture)"
  key_links:
    - from: "main.py"
      to: "api/routes/blueprint.py"
      via: "app.include_router"
---

<objective>
Establish the backend API for architectural metadata and prepare the frontend navigation to support a dedicated "Architecture" view.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-architectural-observability/RESEARCH.md
@.planning/phases/04-architectural-observability/04-CONTEXT.md
@main.py
@frontend-v2/src/App.tsx
@frontend-v2/src/components/Navigation/PillNavbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/blueprint backend route</name>
  <files>api/routes/blueprint.py, main.py</files>
  <action>
    - Create `api/routes/blueprint.py` using FastAPI APIRouter.
    - Implement `GET /api/blueprint` returning:
      - `execution_flow`: A Mermaid string mapping the exact 7 steps from RESEARCH.md: Classify -> Triage -> RAG Retrieval -> Ranking -> Resolution -> Judge -> Automation.
      - `dictionary`: Full list of 8 storage-mapped terms from RESEARCH.md. **Include `nodeId` field (e.g., 'step1')** for each term to support frontend highlighting.
      - `dependencies`: List of links mapping high-level file links.
    - Register the router in `main.py`.
  </action>
  <verify>
    <automated>curl -s http://localhost:8000/api/blueprint | jq '.dictionary[0] | has("nodeId")'</automated>
  </verify>
  <done>/api/blueprint returns structured architectural metadata with nodeId mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Implement View Toggling in Navbar and App</name>
  <files>frontend-v2/src/components/Navigation/PillNavbar.tsx, frontend-v2/src/App.tsx</files>
  <action>
    - Update `PillNavbar.tsx`:
      - Add `blueprint` to the `sections` array with label "Blueprint".
      - Modify `onClick` to handle a new `onViewChange` prop if the clicked section is 'blueprint'.
    - Update `App.tsx`:
      - Add `activeView` state ('operations' | 'architecture').
      - Update `PillNavbar` call to include `onViewChange={(view) => setActiveView(view)}`.
      - Wrap current Operational UI in a conditional block: `{activeView === 'operations' && (...)}`.
      - Add a placeholder for `{activeView === 'architecture' && <div id="blueprint-root">Architecture View Skeleton</div>}`.
  </action>
  <verify>
    <automated>grep "activeView" frontend-v2/src/App.tsx</automated>
  </verify>
  <done>User can toggle between Operations and Architecture views via the Navbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Architecture metadata exposed to client |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | /api/blueprint | accept | Metadata is documentation, not sensitive user data or secrets. |
</threat_model>

<success_criteria>
1. `curl http://localhost:8000/api/blueprint` returns the 7-step Mermaid string and term dictionary.
2. Clicking "Blueprint" in the navbar hides the dashboard and shows the "Architecture View Skeleton".
</success_criteria>

<output>
After completion, create `.planning/phases/04-architectural-observability/04-01-SUMMARY.md`
</output>
