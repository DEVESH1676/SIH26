---
phase: 04-architectural-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [frontend-v2/package.json, frontend-v2/src/components/Architecture/Mermaid.tsx, frontend-v2/src/components/Architecture/TermInspector.tsx]
autonomous: true
requirements: [UI-06, UI-07]

must_haves:
  truths:
    - "Mermaid library is installed and rendering SVG"
    - "TermInspector handles search input and list filtering"
  artifacts:
    - path: "frontend-v2/src/components/Architecture/Mermaid.tsx"
      provides: "Universal Mermaid renderer component"
    - path: "frontend-v2/src/components/Architecture/TermInspector.tsx"
      provides: "Searchable term dictionary list"
---

<objective>
Install Mermaid.js dependencies and build the core reusable components for the Nexus Blueprint visualization.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-architectural-observability/RESEARCH.md
@frontend-v2/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install beautiful-mermaid and build Mermaid.tsx</name>
  <files>frontend-v2/package.json, frontend-v2/src/components/Architecture/Mermaid.tsx</files>
  <action>
    - Install `beautiful-mermaid` (v1.1.3).
    - Create `frontend-v2/src/components/Architecture/Mermaid.tsx`:
      - Import `Mermaid` from `beautiful-mermaid`.
      - Export a wrapper `MermaidCanvas` that accepts a `chart` string and a `className`.
      - Apply glassmorphism styling to the container: `glass border-white/10 rounded-2xl p-8`.
  </action>
  <verify>
    <automated>grep -q "beautiful-mermaid" frontend-v2/package.json</automated>
  </verify>
  <done>Mermaid renderer component is available with glassmorphism styles.</done>
</task>

<task type="auto">
  <name>Task 2: Create searchable TermInspector</name>
  <files>frontend-v2/src/components/Architecture/TermInspector.tsx</files>
  <action>
    - Create `frontend-v2/src/components/Architecture/TermInspector.tsx`:
      - Accept `terms` (from the API) as a prop.
      - Add a search input with glassmorphism styling.
      - Filter terms based on input.
      - Render each term card with: Name, Logical Meaning, Code Path (Badge), and **Storage Location** (DB table/column).
      - Implement `onSearch(term)` callback.
  </action>
  <verify>
    <automated>grep -E "storage_path|storagePath" frontend-v2/src/components/Architecture/TermInspector.tsx</automated>
  </verify>
  <done>TermInspector component can search and filter the system dictionary with full D-04 metadata.</done>
</task>

</tasks>

<success_criteria>
1. `beautiful-mermaid` is present in `package.json`.
2. `Mermaid.tsx` and `TermInspector.tsx` exist and export functional React components.
</success_criteria>

<output>
After completion, create `.planning/phases/04-architectural-observability/04-02-SUMMARY.md`
</output>
