---
phase: 02-frontend-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [frontend-v2/src/layouts/MainLayout.tsx, frontend-v2/src/components/Navigation/PillNavbar.tsx, frontend-v2/src/components/Pulse/HealthPulse.tsx, frontend-v2/src/components/History/HistoryMock.tsx, frontend-v2/src/components/Analytics/AnalyticsMock.tsx]
autonomous: true
requirements: [UI-01, UI-03]

must_haves:
  truths:
    - "MainLayout implements the split-panel workspace with glassmorphism"
    - "HealthPulse renders a real chart (Recharts) with mocked system metrics"
    - "History and Analytics sections contain actual mocked UI components, not placeholders"
    - "PillNavbar tracks all application sections including History and Analytics"
  artifacts:
    - path: "frontend-v2/src/components/Pulse/HealthPulse.tsx"
      provides: "System Health Pulse panel with metric charts"
    - path: "frontend-v2/src/components/History/HistoryMock.tsx"
      provides: "Mocked ticket run history UI"
    - path: "frontend-v2/src/components/Analytics/AnalyticsMock.tsx"
      provides: "Mocked system performance analytics UI"
---

<objective>
Implement the core application shell, including the split-view layout, status-aware navigation, and high-fidelity mocked components for the extended product surface (Pulse, History, Analytics).

Purpose: Establish the full-page aesthetic and layout before adding live data flows.
Output: A functional UI shell with mocked intelligence dashboards and metrics.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-scaffolding/02-CONTEXT.md
@.planning/phases/02-frontend-scaffolding/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MainLayout (Split Workspace Structure)</name>
  <files>frontend-v2/src/layouts/MainLayout.tsx</files>
  <action>
    Create the `MainLayout` component implementing the D-02 balanced hybrid layout.
    Split view (Above the fold):
    - Left: Command Center (Workspace for form and logs).
    - Right: System Health Pulse (Integration point for Task 2).
    Below the fold:
    - History Section (Integration point for Task 3).
    - Analytics Section (Integration point for Task 3).
    Apply glassmorphism styles (blur, subtle border) to all panels.
  </action>
  <verify>
    <automated>test -f frontend-v2/src/layouts/MainLayout.tsx</automated>
  </verify>
  <done>MainLayout implemented with split-panel structure and glassmorphism.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HealthPulse (Recharts Integration)</name>
  <files>frontend-v2/src/components/Pulse/HealthPulse.tsx</files>
  <action>
    Implement the `HealthPulse` component (UI-03, D-02).
    Use `recharts` to render a "System Load" or "Confidence Trend" AreaChart or LineChart.
    Include 2-3 metric cards (e.g., "Uptime: 99.9%", "Avg Confidence: 0.88").
    Style with a premium "holographic" or "glass" look to communicate an agentic operations feel.
  </action>
  <verify>
    <automated>grep "recharts" frontend-v2/src/components/Pulse/HealthPulse.tsx</automated>
  </verify>
  <done>HealthPulse component implemented with functional charts and metrics.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Mocked History and Analytics Components</name>
  <files>frontend-v2/src/components/History/HistoryMock.tsx, frontend-v2/src/components/Analytics/AnalyticsMock.tsx</files>
  <action>
    Per feedback, implement actual mocked components instead of placeholders (D-06).
    `HistoryMock.tsx`: Render a list of 3-5 mocked ticket runs with status badges (Classified, Escalated, Resolved) and timestamps.
    `AnalyticsMock.tsx`: Render a Bento-grid style layout with mocked performance cards (e.g., "Tokens Processed", "Mean Time to Resolve", "Safety Gate Activations").
    Use Shadcn UI `Table`, `Card`, and `Badge` components.
  </action>
  <verify>
    <automated>grep "HistoryMock" frontend-v2/src/components/History/HistoryMock.tsx && grep "AnalyticsMock" frontend-v2/src/components/Analytics/AnalyticsMock.tsx</automated>
  </verify>
  <done>History and Analytics sections implemented with rich mocked content.</done>
</task>

<task type="auto">
  <name>Task 4: Implement Status-Aware PillNavbar</name>
  <files>frontend-v2/src/components/Navigation/PillNavbar.tsx</files>
  <action>
    Create the `PillNavbar` component with scroll-to-section support (D-05).
    Include pills for: Classify, Triage, RAG, Resolve, Judge, History, Analytics.
    Implement active section tracking using an Intersection Observer or similar viewport tracking.
    Ensure pills reflect a "completed" or "idle" state visually.
  </action>
  <verify>
    <automated>grep "History" frontend-v2/src/components/Navigation/PillNavbar.tsx</automated>
  </verify>
  <done>PillNavbar implemented with comprehensive section tracking and status awareness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Layout | Responsiveness and overflow management in the split-view layout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | D | Charts/Layout | accept | Minor layout shifts during chart resizing are acceptable for the initial scaffolding |
</threat_model>

<verification>
Check visual layout: Split view shows HealthPulse on the right.
Scroll down: History and Analytics show mocked data tables and cards.
Click Navbar: Verify it scrolls to the correct section and highlights the active pill.
</verification>

<success_criteria>
- `MainLayout` uses a split-view workspace.
- `HealthPulse` features Recharts-based metrics.
- `History` and `Analytics` sections have rich mocked content (D-06).
- `PillNavbar` tracks all 7 major sections.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-scaffolding/02-02-SUMMARY.md`
</output>
