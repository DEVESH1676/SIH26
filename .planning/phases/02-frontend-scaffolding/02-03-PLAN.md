---
phase: 02-frontend-scaffolding
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [frontend-v2/src/hooks/usePipeline.ts, frontend-v2/src/components/Submission/TicketForm.tsx, frontend-v2/src/components/Pipeline/GlowingProgressBar.tsx, frontend-v2/src/App.tsx]
autonomous: true
requirements: [UI-02, UI-03]

must_haves:
  truths:
    - "usePipeline hook handles SSE stream with proper cleanup (es.close())"
    - "TicketForm uses Zod validation for required ticket fields"
    - "GlowingProgressBar animates based on 0.0-1.0 progress values from SSE"
    - "App state reflects pipeline transitions (idle -> running -> complete)"
  artifacts:
    - path: "frontend-v2/src/hooks/usePipeline.ts"
      provides: "SSE-driven pipeline state orchestration"
    - path: "frontend-v2/src/components/Submission/TicketForm.tsx"
      provides: "Validated ticket entry form"
    - path: "frontend-v2/src/components/Pipeline/GlowingProgressBar.tsx"
      provides: "Frosted-glass real-time progress indicator"
---

<objective>
Implement the reactive pipeline state layer and the primary command console interaction, including the submission form and the real-time progress indicator.

Purpose: Create the "Nerve Center" and interaction loop for the Nexus AI intelligence pipeline.
Output: Functional SSE state management, a validated form, and a high-fidelity progress bar.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-scaffolding/02-CONTEXT.md
@.planning/phases/02-frontend-scaffolding/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement usePipeline Hook with SSE Cleanup</name>
  <files>frontend-v2/src/hooks/usePipeline.ts</files>
  <action>
    Create a `usePipeline` hook using `useReducer` to manage the multi-stage pipeline state (D-07).
    State must track: `status` (idle, running, completed, error), `progress` (0.0-1.0), `stages` (data payload per stage), and `logs`.
    Implement `EventSource` consumption for the Phase 1 `/api/pipeline/stream` endpoint (D-04).
    CRITICAL: Ensure `es.close()` is called in the `useEffect` cleanup function.
    Support a `mockMode` for development that simulates SSE events with delays (D-06).
    Integrate generated types from `src/types/api.ts` (D-10).
  </action>
  <verify>
    <automated>grep "es.close()" frontend-v2/src/hooks/usePipeline.ts</automated>
  </verify>
  <done>usePipeline hook implemented with robust resource management and type safety.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TicketForm with Zod Validation</name>
  <files>frontend-v2/src/components/Submission/TicketForm.tsx</files>
  <action>
    Implement the `TicketForm` component for the Left Panel (D-03).
    Use Shadcn UI `Textarea` for ticket description and `Button` for submission.
    Apply Zod validation to ensure the ticket content is not empty.
    On submit, trigger the `startPipeline` method from `usePipeline`.
    Handle the 'running' state by disabling the submit button.
  </action>
  <verify>
    <automated>test -f frontend-v2/src/components/Submission/TicketForm.tsx</automated>
  </verify>
  <done>Validated ticket submission form implemented and connected to the pipeline state.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Glowing Frosted-Glass Progress Bar</name>
  <files>frontend-v2/src/components/Pipeline/GlowingProgressBar.tsx</files>
  <action>
    Implement a `GlowingProgressBar` component (D-04).
    Style using Tailwind: `backdrop-blur-md`, `bg-white/10`, and a `1px` subtle border.
    The progress fill should have a "glowing" effect (e.g., `shadow-[0_0_15px_rgba(var(--primary),0.5)]`).
    Animate the width using `framer-motion` based on the `progress` (0.0 to 1.0) from `usePipeline`.
  </action>
  <verify>
    <automated>grep "motion" frontend-v2/src/components/Pipeline/GlowingProgressBar.tsx</automated>
  </verify>
  <done>High-fidelity progress bar implemented with real-time motion and glassmorphism.</done>
</task>

<task type="auto">
  <name>Task 4: Assemble App State and Layout Wiring</name>
  <files>frontend-v2/src/App.tsx</files>
  <action>
    Update `App.tsx` to initialize the `usePipeline` hook.
    Pass state and handlers to `MainLayout`, `TicketForm`, and `GlowingProgressBar`.
    Ensure the `GlowingProgressBar` is visible during the 'running' status.
  </action>
  <verify>
    <automated>grep "usePipeline" frontend-v2/src/App.tsx</automated>
  </verify>
  <done>Application state wired to UI components across the layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE Stream → App State | Reducer must handle unexpected or malformed stream payloads without crashing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | D | usePipeline | mitigate | Use defensive parsing and type guards for all incoming SSE data |
</threat_model>

<verification>
Start dev server, open in browser.
Fill the form and click Submit (using Mock mode).
Verify progress bar animates smoothly from 0 to 100%.
Verify submit button disables while running.
Check console for logs during the pipeline run.
</verification>

<success_criteria>
- `usePipeline` manages state and cleans up SSE connections.
- `TicketForm` validates input and triggers the run.
- `GlowingProgressBar` provides visual feedback for pipeline progress (D-04).
- Application transitions through pipeline states correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-scaffolding/02-03-SUMMARY.md`
</output>
