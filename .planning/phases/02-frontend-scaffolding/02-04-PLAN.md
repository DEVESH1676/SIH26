---
phase: 02-frontend-scaffolding
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: [frontend-v2/src/components/Pipeline/IntelligenceFeed.tsx, frontend-v2/src/components/Pipeline/StageCard.tsx, frontend-v2/src/components/Visuals/TerminalLogs.tsx, frontend-v2/src/components/Visuals/AuroraBackground.tsx, frontend-v2/src/index.css]
autonomous: true
requirements: [UI-02]

must_haves:
  truths:
    - "Intelligence Feed renders stage cards progressively as SSE data arrives"
    - "TerminalLogs component displays live reasoning logs from the pipeline state"
    - "AuroraBackground is functional and uses GPU acceleration (will-change)"
    - "AuroraBackground pauses/reduces when off-screen (useInView)"
  artifacts:
    - path: "frontend-v2/src/components/Pipeline/IntelligenceFeed.tsx"
      provides: "Vertical progressive disclosure feed container"
    - path: "frontend-v2/src/components/Visuals/TerminalLogs.tsx"
      provides: "Live terminal-style reasoning feed"
    - path: "frontend-v2/src/components/Visuals/AuroraBackground.tsx"
      provides: "Optimized premium background effect"
---

<objective>
Implement the primary visualization layer of the intelligence pipeline and apply premium visual treatments (Aurora background, live logs) to the application shell.

Purpose: Complete the "Elite Shell" vision by making the AI's progressive reasoning visible and aesthetically premium.
Output: Dynamic intelligence feed, live reasoning console, and a high-performance animated background.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-scaffolding/02-CONTEXT.md
@.planning/phases/02-frontend-scaffolding/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement IntelligenceFeed & StageCards</name>
  <files>frontend-v2/src/components/Pipeline/IntelligenceFeed.tsx, frontend-v2/src/components/Pipeline/StageCard.tsx</files>
  <action>
    Create the `IntelligenceFeed` component to map over the pipeline stages from `usePipeline`.
    Implement `StageCard.tsx` for displaying results of Classify, Triage, RAG, Resolve, and Judge stages.
    Use `framer-motion` for "Progressive Disclosure" (reveal transitions as stages complete).
    Apply glassmorphism and blur-to-focus transitions (D-09).
    Add holographic linear-gradient borders to "active" or "completed" cards (D-08).
  </action>
  <verify>
    <automated>grep "motion" frontend-v2/src/components/Pipeline/StageCard.tsx</automated>
  </verify>
  <done>Intelligence feed components implemented with animated stage reveals.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Live Terminal Logs</name>
  <files>frontend-v2/src/components/Visuals/TerminalLogs.tsx</files>
  <action>
    Create the `TerminalLogs` component to be placed in the Left "Command Center" panel (D-03).
    Subscribe to the `logs` array from `usePipeline` state.
    Style as a monospaced terminal with auto-scroll to the bottom.
    Apply a subtle glass treatment to the container to match the shell aesthetic.
  </action>
  <verify>
    <automated>test -f frontend-v2/src/components/Visuals/TerminalLogs.tsx</automated>
  </verify>
  <done>Live terminal logs implemented for displaying autonomous reasoning.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Optimized Aurora Background</name>
  <files>frontend-v2/src/components/Visuals/AuroraBackground.tsx, frontend-v2/src/index.css</files>
  <action>
    Implement the `AuroraBackground` component inspired by Aceternity UI (D-08).
    CRITICAL PERFORMANCE OPTIMIZATIONS (per RESEARCH.md Pitfall 1):
    1. Use `will-change-transform` on the animated divs to ensure GPU acceleration.
    2. Use `useInView` from `framer-motion` to stop or reduce animation when not visible.
    3. Animate only `opacity` and `transform` properties.
    Define the aurora color palette in `index.css` using Tailwind variables.
    Wrap the entire application in the `AuroraBackground` in `App.tsx` or `MainLayout`.
  </action>
  <verify>
    <automated>grep "will-change-transform" frontend-v2/src/components/Visuals/AuroraBackground.tsx</automated>
  </verify>
  <done>High-performance premium background effect implemented and integrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side Rendering | Intensive animations must not block the UI thread during data processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | D | Animations | mitigate | Use GPU acceleration and viewport awareness to minimize CPU/battery impact |
</threat_model>

<verification>
Trigger a pipeline run (Mock mode):
1. Verify terminal logs scroll in the left panel.
2. Verify stage cards reveal progressively in the right panel.
3. Check CPU usage in browser devtools: Verify Aurora background doesn't spike CPU.
4. Verify "glowing" effects and glassmorphism look correct.
</verification>

<success_criteria>
- Intelligence Feed renders stages with high-end animations (D-09).
- Terminal logs stream in real-time.
- Aurora background is functional and performant (per Research).
- Final UI shell matches the "Elite Shell" design vision.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-scaffolding/02-04-SUMMARY.md`
</output>
