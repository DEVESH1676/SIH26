---
phase: 03-frontend-robustness
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [frontend-v2/src/components/Command/TicketForm.tsx]
autonomous: true
requirements: [UI-04]

must_haves:
  truths:
    - "Launch button is disabled if Subject < 3 or Description < 10 characters"
    - "Inline validation messages appear when fields are invalid"
  artifacts:
    - path: "frontend-v2/src/components/Command/TicketForm.tsx"
      provides: "Zod-validated form with inline feedback"
  key_links:
    - from: "TicketForm.tsx"
      to: "Backend (FastAPI)"
      via: "onSubmit -> startPipeline"
---

<objective>
Implement the "Validation Wall" to prevent malformed tickets from reaching the backend. This improves UX by providing immediate feedback and prevents backend 422 errors.

Purpose: Client-side validation using Zod.
Output: Validated TicketForm component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-robustness/03-CONTEXT.md

<interfaces>
From frontend-v2/src/components/Command/TicketForm.tsx:
```typescript
interface TicketFormProps {
  onSubmit: (title: string, description: string) => void;
  isLoading: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Zod Validation Logic</name>
  <files>frontend-v2/src/components/Command/TicketForm.tsx</files>
  <behavior>
    - Form should be invalid if title.length < 3.
    - Form should be invalid if description.length < 10.
    - Errors should be accessible in the component state.
  </behavior>
  <action>
    - Define a Zod schema for the ticket: `{ title: z.string().min(3, "Minimum 3 characters required"), description: z.string().min(10, "Minimum 10 characters required") }`.
    - Use state to track validation errors.
    - Validate inputs on change or blur (per D-01).
  </action>
  <verify>
    <automated>grep "z.string().min(3)" frontend-v2/src/components/Command/TicketForm.tsx</automated>
  </verify>
  <done>Zod schema implemented and integrated into form state.</done>
</task>

<task type="auto">
  <name>Task 2: UI Inline Feedback & Button Guard</name>
  <files>frontend-v2/src/components/Command/TicketForm.tsx</files>
  <action>
    - Render red error text below Subject and Description fields if validation fails.
    - Disable the "Launch Intelligence Run" button if the form is invalid or loading.
    - Apply transition animations to error messages for polish.
  </action>
  <verify>
    <automated>grep "disabled={.*|| !isValid}" frontend-v2/src/components/Command/TicketForm.tsx</automated>
  </verify>
  <done>Form UI displays inline errors and prevents submission of invalid data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> UI | Input validation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | TicketForm.tsx | accept | Error messages only reveal length requirements, which is intended behavior. |
</threat_model>

<success_criteria>
1. Launch button disabled until valid (title >= 3, desc >= 10).
2. Inline error messages appear correctly.
3. Form does not submit invalid data to `onSubmit`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-robustness/03-02-SUMMARY.md`
</output>
