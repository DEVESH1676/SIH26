---
phase: 03-frontend-robustness
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified: [frontend-v2/src/components/Pipeline/StageCard.tsx]
autonomous: false
requirements: [UI-05]

must_haves:
  truths:
    - "StageCard displays rich views (Category Badge, Priority Level, RAG Snippet, Step List, Safety Score) instead of raw JSON"
    - "Holographic blur-to-focus animation reveals stage data when result is present"
  artifacts:
    - path: "frontend-v2/src/components/Pipeline/StageCard.tsx"
      provides: "High-fidelity AI stage rendering"
  key_links:
    - from: "StageCard.tsx"
      to: "AnimatePresence"
      via: "motion components"
---

<objective>
Elevate the Intelligence Feed by replacing raw JSON data with rich, semantically meaningful components. Implement a signature "Holographic" animation to reveal AI insights.

Purpose: Elite UX data presentation and animation polish.
Output: Enhanced StageCard component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-robustness/03-CONTEXT.md

<interfaces>
From frontend-v2/src/components/Pipeline/StageCard.tsx:
```typescript
interface StageCardProps {
  stage: string;
  title: string;
  status: 'idle' | 'running' | 'completed' | 'error';
  result?: any;
  index: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Rich Sub-Components</name>
  <files>frontend-v2/src/components/Pipeline/StageCard.tsx</files>
  <action>
    Create internal render functions for each stage type per D-03:
    - **Classification:** Category Badge + Confidence % text.
    - **Triage:** Priority Level (Color-coded) + Rationale text.
    - **RAG:** Source count + Best match snippet (truncated).
    - **Resolution:** Render technical steps as a list.
    - **Judge:** Safety verdict (Verified/Alert) + Rubric scores.
    Replace the `<pre>` tag with conditional rendering of these views.
  </action>
  <verify>
    <automated>grep "renderClassification" frontend-v2/src/components/Pipeline/StageCard.tsx</automated>
  </verify>
  <done>StageCard renders high-fidelity sub-components based on result content.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Holographic Reveal Animation</name>
  <files>frontend-v2/src/components/Pipeline/StageCard.tsx</files>
  <action>
    Refine the `AnimatePresence` motion config per D-04:
    - Update `initial` state: `{ opacity: 0, height: 0, filter: 'blur(10px)', scale: 0.95 }`.
    - Update `animate` state: `{ opacity: 1, height: 'auto', filter: 'blur(0px)', scale: 1 }`.
    - Use `transition={{ type: "spring", stiffness: 100, damping: 20 }}`.
    Ensure staggered entrance for internal elements if possible.
  </action>
  <verify>
    <automated>grep "blur(10px)" frontend-v2/src/components/Pipeline/StageCard.tsx</automated>
  </verify>
  <done>Stage data reveals with a smooth blur-to-focus holographic effect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Rich data presentation and holographic reveal animations in the Intelligence Feed.</what-built>
  <how-to-verify>
    1. Run `npm --prefix frontend-v2 run dev`.
    2. Open the UI and submit a ticket.
    3. Watch each stage expand.
    4. Verify:
       - Classification shows a badge (e.g. "Network").
       - Triage shows a priority (e.g. "High").
       - RAG shows context sources.
       - Resolution shows steps.
       - Judge shows a safety score.
       - Content "defocuses" into view with a blur effect.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | StageCard.tsx | accept | Visual rendering of untrusted data is safe within text nodes; no dangerouslySetInnerHTML is used. |
</threat_model>

<success_criteria>
1. No raw JSON visible in the feed.
2. Distinct UI elements for Category, Priority, and Steps.
3. Blur animation is clearly visible during reveal.
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-robustness/03-03-SUMMARY.md`
</output>
