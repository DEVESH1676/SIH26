---
phase: 03-frontend-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend-v2/src/types/pipeline.ts, frontend-v2/src/hooks/usePipeline.tsx, frontend-v2/src/components/Pipeline/IntelligenceFeed.tsx, frontend-v2/src/components/Pipeline/StageCard.tsx]
autonomous: true
requirements: [UI-05]

must_haves:
  truths:
    - "Pipeline stage IDs are unified on: classification, triage, rag, resolution, judge"
    - "SET_RESULT action correctly updates the results state using backend-provided keys"
    - "Stage transitions in the reducer follow the unified mapping"
  artifacts:
    - path: "frontend-v2/src/types/pipeline.ts"
      provides: "Unified pipeline types"
    - path: "frontend-v2/src/hooks/usePipeline.tsx"
      provides: "Corrected pipeline reducer logic"
  key_links:
    - from: "usePipeline.tsx"
      to: "IntelligenceFeed.tsx"
      via: "PipelineContext state"
---

<objective>
Refactor pipeline stage IDs and state management to ensure perfect synchronization between backend SSE events and frontend state. This fixes the "Completed but Empty" state bug.

Purpose: Consistency between backend keys and frontend state keys.
Output: Refactored types, hooks, and components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-robustness/03-CONTEXT.md

<interfaces>
From frontend-v2/src/types/pipeline.ts:
```typescript
export type PipelineStage = 'idle' | 'classify' | 'triage' | 'rag' | 'resolve' | 'judge' | 'complete' | 'error';
export interface PipelineState {
  stage: PipelineStage;
  results: {
    classification?: any;
    triage?: any;
    rag?: any;
    resolution?: any;
    judge?: any;
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify Pipeline Stage Types</name>
  <files>frontend-v2/src/types/pipeline.ts</files>
  <action>
    Update PipelineStage type and PipelineState.results keys per D-02:
    - Change 'classify' to 'classification'.
    - Change 'resolve' to 'resolution'.
    - Ensure all keys match: classification, triage, rag, resolution, judge.
  </action>
  <verify>
    <automated>grep -E "classification|resolution" frontend-v2/src/types/pipeline.ts</automated>
  </verify>
  <done>Types updated and exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor Pipeline Reducer & State Sync</name>
  <files>frontend-v2/src/hooks/usePipeline.tsx</files>
  <behavior>
    - SET_RESULT with key 'classification' should update state.results.classification.
    - SET_RESULT with key 'resolution' should update state.results.resolution.
    - SET_RESULT should trigger the correct nextStageMap transition (classification -> triage -> rag -> resolution -> judge -> complete).
  </behavior>
  <action>
    - Update `nextStageMap` in `pipelineReducer` to use 'classification' and 'resolution' instead of 'classify' and 'resolve'.
    - Ensure `SET_RESULT` correctly handles all unified keys.
    - Update `dispatch({ type: 'START' })` initial state if it references old keys (e.g., 'classify').
  </action>
  <verify>
    <automated>grep -E "classification|resolution" frontend-v2/src/hooks/usePipeline.tsx</automated>
  </verify>
  <done>Reducer logic synchronized with unified stage IDs.</done>
</task>

<task type="auto">
  <name>Task 3: Update Intelligence Feed & Component Mapping</name>
  <files>frontend-v2/src/components/Pipeline/IntelligenceFeed.tsx, frontend-v2/src/components/Pipeline/StageCard.tsx</files>
  <action>
    - Update `STAGES` constant in `IntelligenceFeed.tsx` to use 'classification' and 'resolution' IDs.
    - Update `getResult` helper in `IntelligenceFeed.tsx` to map keys correctly.
    - Update `getIcon` helper in `StageCard.tsx` to use 'classification' and 'resolution' in the switch statement to prevent broken icons.
  </action>
  <verify>
    <automated>grep -E "classification|resolution" frontend-v2/src/components/Pipeline/IntelligenceFeed.tsx frontend-v2/src/components/Pipeline/StageCard.tsx</automated>
  </verify>
  <done>IntelligenceFeed and StageCard correctly map and display unified stage data and icons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend -> Frontend | SSE result payload keys must be validated by the reducer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | usePipeline.tsx | mitigate | Use TypeScript strict typing for reducer actions to prevent invalid state keys. |
</threat_model>

<success_criteria>
1. All stage IDs unified as: classification, triage, rag, resolution, judge.
2. No "classify" or "resolve" stage strings remain in the core state logic.
3. Icons in StageCard remain functional after refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-robustness/03-01-SUMMARY.md`
</output>
