---
phase: 04-architectural-observability
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: [frontend-v2/src/pages/Blueprint.tsx, frontend-v2/src/App.tsx]
autonomous: true
requirements: [UI-06, UI-07, UI-08]

must_haves:
  truths:
    - "Blueprint page fetches data from /api/blueprint"
    - "Dual-pane layout renders TermInspector and Mermaid graph"
    - "Searching highlights Mermaid nodes using querySelectorAll('.node')"
  artifacts:
    - path: "frontend-v2/src/pages/Blueprint.tsx"
      provides: "Main architecture visualization page"
---

<objective>
Assemble the Nexus Blueprint page with real-time data from the backend and implement the search-to-highlight architectural logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-architectural-observability/RESEARCH.md
@.planning/phases/04-architectural-observability/04-01-PLAN.md
@.planning/phases/04-architectural-observability/04-02-PLAN.md
@frontend-v2/src/components/Architecture/Mermaid.tsx
@frontend-v2/src/components/Architecture/TermInspector.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Blueprint Page and Dual-Pane Layout</name>
  <files>frontend-v2/src/pages/Blueprint.tsx, frontend-v2/src/App.tsx</files>
  <action>
    - Create `frontend-v2/src/pages/Blueprint.tsx`:
      - Fetch data from `http://localhost:8000/api/blueprint` on mount.
      - Store `execution_flow`, `dictionary`, and `dependencies` (for UI-08) in state.
      - Implement a dual-pane layout:
        - Left: `TermInspector`.
        - Right: 
          - Primary `MermaidCanvas` (Execution Flow).
          - **Secondary `MermaidCanvas` (Dependency Graph)** for UI-08 compliance.
      - Add "Nexus Blueprint" glass header.
    - Update `App.tsx`:
      - Replace placeholder with `<Blueprint />`.
  </action>
  <verify>
    <automated>grep -E "dependencies|MermaidCanvas" frontend-v2/src/pages/Blueprint.tsx</automated>
  </verify>
  <done>Nexus Blueprint page is active with both Execution Flow and Dependency Graph rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Term-to-Node Highlight Logic</name>
  <files>frontend-v2/src/pages/Blueprint.tsx</files>
  <action>
    - Implement `handleSearch` with ID mapping:
      - Dictionary terms should now include a `nodeId` field (e.g., 'A' for Classify).
      - Use `querySelectorAll('.node')` to find nodes.
      - Match node ID instead of text content for robustness.
      - Apply `highlight-glow` or `dimmed` CSS properties.
  </action>
  <verify>
    <automated>grep "querySelectorAll" frontend-v2/src/pages/Blueprint.tsx</automated>
  </verify>
  <done>Mermaid nodes highlight robustly via ID mapping when searching terms.</done>
</task>

</tasks>

<success_criteria>
1. Navigating to the Blueprint tab renders the Term Inspector and Mermaid diagram.
2. The Mermaid diagram correctly displays the 7 pipeline steps.
3. Searching a term (e.g., "RAG") dims all nodes except the RAG retrieval node.
</success_criteria>

<output>
After completion, create `.planning/phases/04-architectural-observability/04-03-SUMMARY.md`
</output>
