---
phase: 02-frontend-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/02-frontend-scaffolding/02-VALIDATION.md, frontend-v2/package.json, frontend-v2/vite.config.ts, frontend-v2/src/tests/smoke.test.ts, frontend-v2/src/tests/pipeline.test.ts]
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Phase validation architecture is defined in 02-VALIDATION.md"
    - "Vite dev server starts and serves a React application"
    - "Test environment is configured with Vitest"
    - "Wave 0 smoke and pipeline tests exist as stubs"
  artifacts:
    - path: ".planning/phases/02-frontend-scaffolding/02-VALIDATION.md"
      provides: "Validation framework for Phase 2"
    - path: "frontend-v2/package.json"
      provides: "Project dependencies including vitest and recharts"
    - path: "frontend-v2/src/tests/smoke.test.ts"
      provides: "Stub for UI smoke tests"
    - path: "frontend-v2/src/tests/pipeline.test.ts"
      provides: "Stub for pipeline logic tests"
---

<objective>
Initialize the frontend project foundation and establish the Nyquist-compliant validation framework, including Wave 0 test stubs.

Purpose: Set up the technical and quality baseline for the elite React frontend.
Output: Configured Vite project, validation document, and test stubs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-scaffolding/02-CONTEXT.md
@.planning/phases/02-frontend-scaffolding/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 2 Validation Document</name>
  <files>.planning/phases/02-frontend-scaffolding/02-VALIDATION.md</files>
  <action>
    Create the `02-VALIDATION.md` file following the Nyquist Validation framework.
    Define domain boundaries for the frontend scaffolding (Vite, Layout, SSE Hook, Aurora).
    List invariants:
    - SSE connection MUST close on unmount (T-02-04 mitigation).
    - Aurora background MUST use GPU acceleration (will-change).
    - HealthPulse MUST use Recharts for metrics.
    Specify verification dimensions:
    - D4: Realtime behavior (SSE).
    - D8: Glassmorphism standard.
    - D9: Motion choreography.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-frontend-scaffolding/02-VALIDATION.md</automated>
  </verify>
  <done>Validation framework for Phase 2 is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Vite + React (TypeScript) + Core Dependencies</name>
  <files>frontend-v2/package.json, frontend-v2/vite.config.ts, frontend-v2/tailwind.config.ts</files>
  <action>
    Initialize a new Vite project in `frontend-v2` using the `react-ts` template.
    Install core dependencies: react, react-dom, framer-motion, lucide-react, clsx, tailwind-merge, recharts, zod.
    Install dev dependencies: tailwindcss, postcss, autoprefixer, openapi-typescript, vitest, @vitejs/plugin-react, jsdom.
    Configure `vite.config.ts` with proxy for `/api` to `http://localhost:8000`.
    Add `test` script: `vitest run`.
    Add `generate-types` script: `openapi-typescript http://localhost:8000/openapi.json -o src/types/api.ts`.
    Initialize Tailwind with a "Glass" material utility in `tailwind.config.ts` (backdrop-blur-xl, bg-white/5, border-white/10).
  </action>
  <verify>
    <automated>cd frontend-v2 && npm list react vitest recharts</automated>
  </verify>
  <done>Vite project structure created with all required libraries and test configuration.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 Test Stubs</name>
  <files>frontend-v2/src/tests/smoke.test.ts, frontend-v2/src/tests/pipeline.test.ts</files>
  <action>
    Create `frontend-v2/src/tests/smoke.test.ts` with a basic Vitest test that asserts `true` to verify the test runner works.
    Create `frontend-v2/src/tests/pipeline.test.ts` as a stub for testing the SSE logic in later plans.
    Configure Vitest in `package.json` to use `jsdom` environment.
  </action>
  <verify>
    <automated>cd frontend-v2 && npm test</automated>
  </verify>
  <done>Wave 0 tests created and functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build Environment | Node/NPM ecosystem supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S | API Proxy | mitigate | Proxy only targets local loopback (localhost:8000) |
| T-02-02 | T | Dependencies | mitigate | Use fixed versions or lockfile; npm audit in CI (implied) |
</threat_model>

<verification>
Run `npm run test` in `frontend-v2` and verify stubs pass.
Verify `02-VALIDATION.md` contains the requested invariants.
</verification>

<success_criteria>
- Vite project initialized.
- `02-VALIDATION.md` created with Nyquist invariants.
- `smoke.test.ts` and `pipeline.test.ts` created and passing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-scaffolding/02-01-SUMMARY.md`
</output>
